import { createStyles, Theme, makeStyles } from '@material-ui/core/styles';

export const useSliderFMProfileStyle = makeStyles((theme: Theme) =>
  createStyles({
    carousel: {
      marginTop: '5%',
      marginBottom: '5%',
    },
  })
);
